<Window x:Class="IbClipboardUtil.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IbClipboardUtil"
        mc:Ignorable="d"
        Title="IbClipboardUtil" Height="651.325" Width="884.595">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <avalonEdit:TextEditor
            xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
            Name="textEditor"
            Grid.Row="0"
            FontFamily="Consolas"
            WordWrap="True" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="3">
            <Button x:Name="btnRefresh" Click="btnRefresh_Click" Padding="3">Refresh</Button>
        </StackPanel>
    </Grid>
</Window>
